<%= @environment %>:
  adapter:   <%= @adapter %>
  database:  '<%= @database %>'
  username:  '<%= @username %>'
  password:  '<%= @password %>'
  host:      '<%= @host %>'
  reconnect: true
  pool: 50
  timeout: 5000
